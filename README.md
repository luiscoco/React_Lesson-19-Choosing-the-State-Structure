# Travel Planner — React 19 + Vite

A small app to practice *Choosing the State Structure* in React:
- Group related state
- Avoid contradictions
- Avoid redundant/duplicate state
- Flatten deeply nested structures

## Run locally

```bash
npm install
npm run dev
```

Then open the printed local URL (usually http://localhost:5173).

## Build for production

```bash
npm run build
npm run preview
```
